# Playground

A collection of Unity pet projects.

## Projects

- [worm](projects/worm) — a grid-based worm (snake) game built with Unity 6000.5.6f1 and the Universal Render Pipeline.

## Structure

Each pet project lives in its own directory under `projects/`, as a self-contained Unity project.
